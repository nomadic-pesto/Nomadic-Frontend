import { maxWidth } from "@mui/system";

const styles = {

    textAlign: {
        xl: "left !important",
        ld: "left !important",
        md: "left !important",

    }
    
}

export default styles;